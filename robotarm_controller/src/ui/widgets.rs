use anyhow::{Context, Result, anyhow, bail, ensure};
use tracing::{debug, error, info, trace, warn};

use crate::ui::app::App;

/// Tuning widgets
impl App {
    fn pid_tuning_widget(&mut self, ui: &mut egui::Ui) {
        unimplemented!()
    }
}
